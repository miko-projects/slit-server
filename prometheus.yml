global:
  scrape_interval: 10s

scrape_configs:

  - job_name: slit-app
    static_configs:
      - targets: ['app:8080']
        labels:
          instance: slit-app
    metrics_path: /actuator/prometheus
